import { BufferGeometry, Material, Matrix4 } from 'three';
import { Items, IFragment } from './base-types';
import { FragmentMesh } from './fragment-mesh';
import { Blocks } from './blocks';
export declare class Fragment implements IFragment {
    mesh: FragmentMesh;
    capacity: number;
    fragments: {
        [id: string]: Fragment;
    };
    id: string;
    blocks: Blocks;
    items: number[];
    hiddenInstances: {
        [id: number]: Items;
    };
    constructor(geometry: BufferGeometry, material: Material | Material[], count: number);
    dispose(disposeResources?: boolean): void;
    getItemID(instanceID: number, blockID: number): number;
    getInstanceAndBlockID(itemID: number): {
        instanceID: number;
        blockID: number;
    };
    getVertexBlockID(geometry: BufferGeometry, index: number): number;
    getItemData(itemID: number): {
        instanceID: number;
        blockID: number;
    };
    getInstance(instanceID: number, matrix: Matrix4): void;
    setInstance(instanceID: number, items: Items): void;
    addInstances(items: Items[]): void;
    removeInstances(itemsIDs: number[]): void;
    clear(): void;
    addFragment(id: string, material?: Material[]): Fragment;
    removeFragment(id: string): void;
    resetVisibility(): void;
    setVisibility(itemIDs: number[], visible: boolean): void;
    resize(size: number): void;
    export(): Promise<{
        geometry: File;
        data: File;
    }>;
    private copyGroups;
    private initializeGeometry;
    private saveItemsInMap;
    private resizeCapacityIfNeeded;
    private createFragmentMeshWithNewSize;
    private disposeNestedFragments;
    private checkBlockNumberValid;
    private checkIfInstanceExist;
    private deleteAndRearrangeInstances;
    private deleteAndRearrange;
    private getItemIndex;
    private getInstanceIDFromIndex;
    private toggleInstanceVisibility;
    private makeInstancesInvisible;
    private makeInstancesVisible;
    private filterHiddenItems;
    private toggleBlockVisibility;
}
